
Amazon


UI -> mobile -> otp send api -> backend (otp, db store -> 15 mins) -> api response
- - - - 


1 2 3 4  -  validate api -> otp, mobile -> backend (mobile otp -> compare, 15 mins) -> success, 

-> MSG91 -> 

database -> project data

users (table) -> id -< numbers>, name text, email id text , mobile text, otp int, otp validity int, password text, signup date date time, blocked or not, ( columns)

products (table) -> title, price, quantity, specifications, images, videos (columns)

cart (table) -> product, user , quantity, created, updated  (columns)

order (table) -> user, products, total price, discount, selling price, payment mode (cod, upi, cc, debit card), order status, created, delivered on, cancelled on, 
          cancellation reason, 

address -> user, flat, city, town, state, pincode, mobile, address type > home or office

whishlist -> product, user, created on

reviews -> product, revieew comment, image, video, is verified, verfied on, user, rating (1 to 5)

amazon_db -> database


Database connection:

URL
user name
password
port number




conversations -> business id, user_id, msg, user_msg or ai reply, created_on











