
Upload file
    -> read data
    -> convert to chunks
    -> convert to vectors
    -> store in db

chat
    -> user query 
    -> query to embedings
    -> search embedings (db)
    -> query, search results, prompt -> llm
    -> llm response

llamaindex ->

5 lines -> 

# pip install llama-index
# pip install llama-index-llms-openai pypdf

# pip install dotenv

OPENAI_API_KEY=dshsh
ANTHROPIC_API_KEY=hshsh



# txt, pdf, doc, md, html files

multi files

