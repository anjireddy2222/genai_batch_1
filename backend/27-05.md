
# tools

# own tools -> with in project

# framework tools -> langchain community tools

# external companies -> google, github, zapier, 

# own tool server -> own company or external companies

# MCP -> server, client

backend project ->
            -> task -> functions -> tool binding

run MCP server


client -> mcp server details -> 

# pip install mcp

uvicorn server:app --reload --host 0.0.0.0 --port 8001

http://localhost:8001/mcp


