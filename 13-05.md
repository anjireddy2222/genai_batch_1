

Scrape websites

# pip install requests beautifulsoup4  -> for scraping server rendred websites -> blogs, server side -> laravel, nextjs

# js rendered websites -> pip  install playwright   -> automation -> 

# playwright install chromium -> headless browser







# ReactJS interview



axios.interceptors.response.use(
  res => res,
  err => {
    if (err.response?.status === 401) logout();
    return Promise.reject(err);
  }
);






